name: GitHub Pages

on:
  push:
    branches:
      - master
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      BASE_PATH: /${{ github.event.repository.name }}/
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: package-lock.json
      - uses: actions/configure-pages@v5
      - name: Install workspace dependencies
        run: npm ci --include=optional
      - name: Check P4B dossier sources
        run: npm run ccsolver:p4b:check
      - name: Check P2B/P6A causal alignment sources
        timeout-minutes: 15
        run: npm run ccsolver:p6a:check
      - name: Build web app for GitHub Pages
        run: npm run build
      - name: Add SPA fallback page
        run: cp web/dist/index.html web/dist/404.html
      - name: Emit unlisted CCSolver dossier bundle
        run: npm run ccsolver:p4b:emit-dist
      - name: Emit unlisted causal alignment bundle
        run: npm run ccsolver:p6a:emit-dist
      - uses: actions/upload-pages-artifact@v3
        with:
          path: web/dist

  deploy:
    if: github.ref == 'refs/heads/master'
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
